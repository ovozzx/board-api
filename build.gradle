plugins {
    id 'java'
    id 'org.springframework.boot' version '2.7.15' // Spring Boot 버전
    id 'io.spring.dependency-management' version '1.0.15.RELEASE'
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'
sourceCompatibility = '11' // 사용 중인 자바 버전에 맞게 수정 (예: 11 또는 17)

repositories {
    mavenCentral()
}

dependencies {
    // Spring Boot Starter (자동완성의 핵심)
    implementation 'org.springframework.boot:spring-boot-starter'
    implementation 'org.springframework.boot:spring-boot-starter-web'

    // JSP
    implementation 'org.apache.tomcat.embed:tomcat-embed-jasper'
    implementation 'javax.servlet:jstl'

    // MySQL
    implementation 'mysql:mysql-connector-java:8.0.33'

    testImplementation 'org.springframework.boot:spring-boot-starter-test'

    implementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter:2.3.1'
    testImplementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter-test:2.3.1'

    // lombok
    compileOnly 'org.projectlombok:lombok:1.18.30'
    annotationProcessor 'org.projectlombok:lombok:1.18.30'

    // swagger
    implementation 'org.springdoc:springdoc-openapi-ui:1.7.0'

    // validation 어노테이션 사용을 위함
    implementation 'org.springframework.boot:spring-boot-starter-validation'

    // MapStruct => 컴파일 타임에 매핑 코드를 자동 생성
    implementation 'org.mapstruct:mapstruct:1.5.5.Final'
    annotationProcessor 'org.mapstruct:mapstruct-processor:1.5.5.Final'
    annotationProcessor 'org.projectlombok:lombok-mapstruct-binding:0.2.0' // Lombok과 함께 쓸 때 필요
}

tasks.named('test') {
    useJUnitPlatform()
}